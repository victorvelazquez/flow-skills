# Implementation Tasks: Repository-Authoritative Multihost Flow Skills

## Review Workload Forecast

| Field | Value |
| ------- | ------- |
| Estimated changed lines | 3,200–4,800 authored additions + deletions; regenerated lock records are additional review identity, not a reason to omit them |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 contracts → PR 2 portable core (read-only/content) → PR 3 portable core (Git mutations) → PR 4 Pi package boundary → PR 5 OpenCode adapter relocation → PR 6 provenance and deploy engine → PR 7 reconciliation, migration, and release gate |
| Delivery strategy | exception-ok |
| Chain strategy | not applicable — single PR |

Decision needed before apply: No — maintainer explicitly accepted `size:exception`
Chained PRs recommended: Yes, declined by explicit exception
Chain strategy: not applicable
400-line budget risk: High

The forecast crosses the review budget and multiple independent review areas. The maintainer explicitly accepted a single-PR `size:exception`; implementation may proceed as one delivery unit. This exception does not authorize commits, pushes, publication, releases, or live-host deployment.

## Execution Rules

- **Dependencies:** Complete a task only after its listed predecessors. Keep its tests and user-visible documentation in the same commit/review unit.
- **Strict TDD for every implementation task:** first add the listed focused behavioral test and run it to demonstrate the expected RED failure; implement the smallest GREEN change; add the named boundary/counterexample (TRIANGULATE); refactor only after focused tests pass. Record each command's literal exit status, pass/fail/skip counts, and skip reasons in apply evidence. A skip is never a pass.
- **Verification:** Run the listed focused command during the task. Run `node --test tests/*.test.mjs` only at the end of each completed proposed review slice and again at the release gate. No package publication or real Pi/OpenCode deployment is authorized by these tasks.
- **Runtime evidence:** For documentation-only tasks, runtime harness is `N/A` because no executable runtime boundary changes. For tool tasks, use the fixture scenario named in the task; the focused `node --test` command is the harness. Record its exact result rather than pre-claiming it here.
- **Lock hygiene:** Regenerate host and generation locks only through the implemented deterministic generator from one clean repository state. Include resulting lock files and `.gitattributes` changes in the same review unit; never hand-edit hashes.

## Decision Gate

- [x] **0. Resolve oversized-delivery handling before apply.** **Decision:** the maintainer explicitly accepted one single PR with `size:exception`. **Verification:** this record preserves the high-risk forecast and confirms that no chain strategy applies. **Authority boundary:** this decision permits an oversized implementation diff only; it does not authorize commits, pushes, publication, releases, reconciliation apply, or live-host deployment. **Runtime harness:** N/A—planning gate only. **Rollback:** no product changes; revert this decision record if the maintainer changes delivery strategy. **Estimate:** 0 lines. **Proposed slice:** single PR under explicit exception.

## PR 1 — Contract Foundation (target: 280–380 lines)

- [x] **1. Define and validate the portable workflow registry and adapter port contract.** **Depends on:** 0. **Files:** add `core/workflows.json` and `core/host-adapter-contract.md`; add `tests/multihost-core.test.mjs`; update `package.json` only if an exported validation entry needs metadata. **RED:** make `tests/multihost-core.test.mjs` reject duplicate/unsorted IDs, missing `SKILL.md`/runtime resources, undeclared host claims, and forbidden host tokens in the registry contract; run `node --test tests/multihost-core.test.mjs`. **GREEN:** add the sorted v1 registry for `flow-audit`, `flow-branch`, `flow-build`, `flow-commit`, `flow-debt`, `flow-docs-sync`, `flow-playbook-sync`, `flow-pr`, `flow-refactor`, `flow-request`, `flow-ui`, and the `ui-design-system` support resource; write the host-port stages (discover through present) without a universal permission assertion. **TRIANGULATE:** test `runtime: null` for a support resource and a host claim whose adapter is absent. **REFACTOR:** centralize test fixture construction without weakening token/path assertions. **Focused verification:** `node --test tests/multihost-core.test.mjs`; then `node --test tests/*.test.mjs` for PR 1. **Runtime harness:** contract fixture discovery and validation; expect focused command exit 0 after GREEN. **Rollback:** delete only `core/` and `tests/multihost-core.test.mjs` (and any isolated metadata export). **Estimate:** 280–380 lines. **Commit/review unit:** `feat(multihost): define portable workflow contracts`.

## PR 2 — Portable Core: Read-Only and Content Workflows (target: 320–400 lines)

- [x] **2. Make read-only workflow contracts portable without changing outcomes.** **Depends on:** 1. **Files:** `skills/flow-audit/SKILL.md`, `skills/flow-refactor/SKILL.md`, `skills/flow-ui/SKILL.md`, `skills/ui-design-system/**`; corresponding OpenCode discovery targets `commands/flow-audit.md`, `commands/flow-refactor.md`, `commands/flow-ui.md`; extend `tests/multihost-core.test.mjs` and add/extend `tests/opencode-adapter.test.mjs`. **RED:** add scanner assertions that shared files contain no OpenCode macros, agent names, OpenCode/Pi install paths, or host permission claims, while adapter fixtures retain host syntax; run `node --test tests/multihost-core.test.mjs tests/opencode-adapter.test.mjs`. **GREEN:** move only portable semantics, runtime contract, safety, and result wording into the shared skills; leave OpenCode invocation/routing in adapter-target files. **TRIANGULATE:** prove a poisoned absent OpenCode path cannot be required by a shared skill resource reference. **REFACTOR:** normalize relative-resource references across this family. **Focused verification:** `node --test tests/multihost-core.test.mjs tests/opencode-adapter.test.mjs`. **Runtime harness:** N/A—these are instruction contracts; retain existing `tests/flow-audit.test.mjs` regressions unchanged. **Rollback:** revert this family’s four shared resource trees and matching adapter/test assertions only. **Estimate:** 320–400 lines. **Commit/review unit:** `refactor(skills): make audit workflows host-neutral`.

- [x] **3. Make content and project-workflow contracts portable.** **Depends on:** 1. **Files:** `skills/flow-build/**`, `skills/flow-debt/SKILL.md`, `skills/flow-docs-sync/SKILL.md`, `skills/flow-playbook-sync/SKILL.md`, `skills/flow-request/SKILL.md`; corresponding OpenCode discovery targets `commands/flow-build.md`, `commands/flow-debt.md`, `commands/flow-docs-sync.md`, `commands/flow-playbook-sync.md`, `commands/flow-request.md`; extend `tests/multihost-core.test.mjs` and `tests/opencode-adapter.test.mjs`. **RED:** add per-registry-resource and forbidden-token tests for this family; run `node --test tests/multihost-core.test.mjs tests/opencode-adapter.test.mjs`. **GREEN:** preserve each workflow’s clarification, preview, approval-required, and failure semantics in host-neutral prose; retain host interaction syntax only in OpenCode targets. **TRIANGULATE:** test instruction-only versus runtime-backed registry entries and a malformed relative resource escaping its skill directory. **REFACTOR:** factor repeated host-neutral safety language into precise local references, not a new host abstraction. **Focused verification:** `node --test tests/multihost-core.test.mjs tests/opencode-adapter.test.mjs`; then `node --test tests/*.test.mjs` for PR 2. **Runtime harness:** N/A—do not rewrite existing deterministic runtime tests solely for this relocation. **Rollback:** revert only the listed skill/adapter family and its tests. **Estimate:** 320–400 lines; split `flow-build/**` by phase-resource subgroup if the first honest diff exceeds the budget. **Commit/review unit:** `refactor(skills): make content workflows host-neutral`.

## PR 3 — Portable Core: Bounded Git/GitHub Workflows (target: 300–390 lines)

- [x] **4. Separate host-neutral branch and commit semantics from OpenCode interaction.** **Depends on:** 1. **Files:** `skills/flow-branch/SKILL.md`, `skills/flow-commit/SKILL.md`, `commands/flow-branch.md`, `commands/flow-commit.md`, `commands/flow-auto-deliver.md`, `agents/flow-branch-agent.md`, `agents/flow-git-agent.md`; extend `tests/multihost-core.test.mjs`, `tests/opencode-adapter.test.mjs`, and move applicable assertions from `tests/flow-agent-contract.test.mjs`. **RED:** assert core content has no OpenCode paths or permission syntax while adapter tests require exact OpenCode `$ARGUMENTS`, routing, and distinct delete/force-delete or commit approval gates; run `node --test tests/multihost-core.test.mjs tests/opencode-adapter.test.mjs tests/flow-agent-contract.test.mjs`. **GREEN:** keep immutable plan/seal/execute and branch safety contracts in shared skills; keep OpenCode questions, agents, and permission patterns in adapters. **TRIANGULATE:** cover unavailable approval and stale plan paths without granting consent from adapter metadata. **REFACTOR:** remove duplicated semantic language from adapters while preserving testable native routing. **Focused verification:** `node --test tests/multihost-core.test.mjs tests/opencode-adapter.test.mjs tests/flow-agent-contract.test.mjs`. **Runtime harness:** existing `tests/flow-branch.test.mjs` and `tests/flow-commit.test.mjs` remain regression evidence; run each if its runtime-facing contract changes. **Rollback:** revert only branch/commit shared contracts, their adapters, and associated assertions. **Estimate:** 300–390 lines. **Commit/review unit:** `refactor(skills): separate Git workflow semantics from OpenCode UX`.

- [x] **5. Separate host-neutral PR semantics from the OpenCode PR adapter.** **Depends on:** 4. **Files:** `skills/flow-pr/SKILL.md`, `skills/flow-pr/references/output-contract.md`, `commands/flow-pr.md`, `agents/flow-pr-agent.md`; extend `tests/multihost-core.test.mjs`, `tests/opencode-adapter.test.mjs`, and `tests/flow-agent-contract.test.mjs`. **RED:** add tests that reject OpenCode installation/agent/permission claims in shared PR resources and require the adapter’s exact native clarification and one-approval routing; run `node --test tests/multihost-core.test.mjs tests/opencode-adapter.test.mjs tests/flow-agent-contract.test.mjs`. **GREEN:** retain the existing prepare, bounded semantic-authoring, immutable approval, execute, postcondition, and result contract, with host interaction isolated to the adapter. **TRIANGULATE:** test a missing approval capability produces `unavailable` with zero mutation and a stale execution identity returns to preview. **REFACTOR:** deduplicate only prose that is already governed by the shared output contract. **Focused verification:** `node --test tests/multihost-core.test.mjs tests/opencode-adapter.test.mjs tests/flow-agent-contract.test.mjs`; then `node --test tests/*.test.mjs` for PR 3. **Runtime harness:** `node --test tests/flow-pr.test.mjs tests/flow-pr-executor.test.mjs` if any runtime-facing wording changes. **Rollback:** revert the PR core/adapter pair and their tests as one unit. **Estimate:** 300–390 lines. **Commit/review unit:** `refactor(skills): make Flow PR semantics host-neutral`.

## PR 4 — Pi Package Boundary (target: 300–390 lines)

- [x] **6. Declare and test Pi package-resource discovery and isolation.** **Depends on:** 1–5. **Files:** `package.json`, add `hosts/pi/flow-assets.json`, add `tests/pi-package.test.mjs`; update `README.md` with Pi-first installation guidance in this same unit. **RED:** create a packed-package fixture test that reads `package.json.pi.skills`, rejects undeclared/malformed/missing `SKILL.md` entries and non-allowlisted package files, and discovers every registry-supported workflow with OpenCode directories absent/poisoned; run `node --test tests/pi-package.test.mjs`. **GREEN:** add Node `>=18`, explicit positive `pi.skills`, exact Pi-consumable `files` allowlist, Pi keywords, and a package-relative host manifest with no writable destination or absolute/sensitive data. **TRIANGULATE:** assert an accidental new `skills/flow-*` directory is not public until declared and that a runtime reference resolves inside the packed package. **REFACTOR:** share registry fixture parsing with `tests/multihost-core.test.mjs` only if it remains explicit at the package boundary. **Focused verification:** `node --test tests/pi-package.test.mjs tests/multihost-core.test.mjs`; then `node --test tests/*.test.mjs` for PR 4. **Runtime harness:** packed-package discovery fixture; expect focused command exit 0 after GREEN; do not install or modify real Pi state. **Rollback:** revert package metadata, Pi manifest, Pi-package test, and the Pi install section together. **Estimate:** 300–390 lines. **Commit/review unit:** `feat(pi): publish declared Flow skill resources`.

- [x] **7. Generate and verify Pi-specific provenance without a live Pi deployment marker.** **Depends on:** 6. **Files:** add `hosts/pi/flow-assets.lock.json`, add `flow-generation.lock.json`, add/extend `tools/lib/asset-generation.mjs`, `tools/lib/asset-contracts.mjs`, `tests/flow-assets-generation.test.mjs`, and `.gitattributes`. **RED:** test reproducible canonical generation IDs, a Pi host identity distinct from OpenCode, package-metadata binding, sorted exact records, and rejection of timestamps/absolute/sensitive fields; run `node --test tests/flow-assets-generation.test.mjs`. **GREEN:** implement the v2 canonical payload and Pi lock generation, excluding host locks and the generation lock from their own payload; update attributes for all deterministic lock bytes. **TRIANGULATE:** test one tampered package field and one changed distributable source record alter/invalidates the correct identity while Pi remains marker-free. **REFACTOR:** isolate canonical JSON/digest helpers from filesystem and CLI effects. **Focused verification:** `node --test tests/flow-assets-generation.test.mjs`. **Runtime harness:** deterministic temporary-repository lock generation; expect focused command exit 0 after GREEN. **Rollback:** remove the Pi lock/generation lock and their generator/attribute/test changes without touching deployed host files. **Estimate:** 300–390 lines. **Commit/review unit:** `feat(assets): add Pi provenance identity`.

## PR 5 — OpenCode Adapter Relocation (target: two units, each 280–390 lines)

- [x] **8. Relocate and verify OpenCode read-only/content adapters with unchanged destination mapping intent.** **Depends on:** 2–3 and 6–7. **Files:** move `commands/flow-audit.md`, `commands/flow-build.md`, `commands/flow-debt.md`, `commands/flow-docs-sync.md`, `commands/flow-playbook-sync.md`, `commands/flow-refactor.md`, `commands/flow-request.md`, and `commands/flow-ui.md` to `hosts/opencode/commands/`; add `hosts/opencode/flow-assets.json`; extend `tests/opencode-adapter.test.mjs` and `tests/legacy-migration.test.mjs`. **RED:** assert each supported registry claim maps to an existing thin adapter and that adapter-only OpenCode syntax is absent from package files; run `node --test tests/opencode-adapter.test.mjs tests/legacy-migration.test.mjs`. **GREEN:** preserve native destination names through explicit source-to-`commands/` mappings, preserve only routing/interaction content in adapters, and declare protected/excluded scopes. **TRIANGULATE:** reject a mapping outside `commands/` or an adapter claiming a workflow missing from the registry. **REFACTOR:** keep repeated routing wording short by linking to the shared core, not by generating adapter prose. **Focused verification:** `node --test tests/opencode-adapter.test.mjs tests/legacy-migration.test.mjs`. **Runtime harness:** adapter mapping fixture; expect focused command exit 0 after GREEN. **Rollback:** move only this adapter family back and delete its host-manifest entries; no live destination is touched. **Estimate:** 280–390 lines. **Commit/review unit:** `refactor(opencode): relocate content adapters`.

- [x] **9. Relocate and verify OpenCode Git/GitHub adapters and agents.** **Depends on:** 4–5 and 8. **Files:** move `commands/flow-branch.md`, `commands/flow-commit.md`, `commands/flow-pr.md`, `commands/flow-auto-deliver.md` to `hosts/opencode/commands/`; move `agents/flow-branch-agent.md`, `agents/flow-git-agent.md`, `agents/flow-pr-agent.md`, `agents/flow-review-agent.md` to `hosts/opencode/agents/`; extend `hosts/opencode/flow-assets.json`, `tests/opencode-adapter.test.mjs`, and `tests/flow-agent-contract.test.mjs`. **RED:** make tests require current adapter mappings, exact `$ARGUMENTS`/agent/question/permission patterns, and no duplicated shared semantic rules; run `node --test tests/opencode-adapter.test.mjs tests/flow-agent-contract.test.mjs`. **GREEN:** relocate sources and map them to unchanged native `commands/`/`agents/` destinations; keep native clarification/approval while shared skills retain outcome semantics. **TRIANGULATE:** prove a missing adapter capability stops as unavailable and a legacy root source path is rejected by the v2 mapping validator. **REFACTOR:** consolidate fixture path construction only. **Focused verification:** `node --test tests/opencode-adapter.test.mjs tests/flow-agent-contract.test.mjs`; then `node --test tests/*.test.mjs` for PR 5. **Runtime harness:** adapter contract fixture; expect focused command exit 0 after GREEN. **Rollback:** restore only this command/agent family and matching manifest entries. **Estimate:** 280–390 lines. **Commit/review unit:** `refactor(opencode): relocate Git workflow adapters`.

## PR 6 — Dual-Host Integrity and Managed Deployment (target: three units, each 300–400 lines)

- [x] **10. Complete v2 host-lock validation and common-generation verification.** **Depends on:** 7–9. **Files:** add `hosts/opencode/flow-assets.lock.json`; extend `tools/lib/asset-contracts.mjs` and `tools/lib/asset-generation.mjs`; update `tools/flow-assets.mjs`; extend `tests/flow-assets-generation.test.mjs`. **RED:** test both locks point to one generation while retaining distinct identities, validate sorted exact source/destination records and protected scopes, and reject cross-host destinations or tampering; run `node --test tests/flow-assets-generation.test.mjs`. **GREEN:** load/validate `flow-host-assets/v2`, `flow-host-assets-lock/v2`, and `flow-generation-lock/v2`, then generate all three locks from a clean tree. **TRIANGULATE:** verify a source wildcard can generate records but never grants deletion ownership and a host-lock identity excludes its self field. **REFACTOR:** keep v1 parsing isolated from v2 schema validation. **Focused verification:** `node --test tests/flow-assets-generation.test.mjs`. **Runtime harness:** temporary repository lock verification; expect focused command exit 0 after GREEN. **Rollback:** revert v2 OpenCode lock/common generation and schema code as one unit; leave v1 reader intact. **Estimate:** 300–400 lines. **Commit/review unit:** `feat(assets): verify dual-host generation locks`.

- [x] **11. Parameterize OpenCode preview/deploy with exact ownership, marker, and recovery safeguards.** **Depends on:** 10. **Files:** add `tools/lib/managed-deployment.mjs`; refactor `tools/flow-assets.mjs`; extend `tests/flow-assets-deploy.test.mjs` (create it by extracting only deploy-specific cases from `tests/flow-assets-restore.test.mjs`); retain regression coverage in `tests/install.test.mjs` and `tests/flow-assets-restore.test.mjs`. **RED:** cover `deploy --host opencode --ref ... --dry-run`, required immutable IDs on apply, stale identity rejection before backup/writes, collision with unowned targets, exact lock-union deletion scope, marker write, backup/recovery, literal Git argv, and protected host files; run `node --test tests/flow-assets-deploy.test.mjs`. **GREEN:** route only `--host opencode` through the parameterized existing transaction safeguards and write `.flow-skills/hosts/opencode.json` transactionally after exact postcondition verification. **TRIANGULATE:** prove missing/malformed marker fails closed and current v1 historical refs still use the strict read-only fallback reader. **REFACTOR:** move algorithms only when parameterization requires it; retain current safety behavior and avoid a general host-permission API. **Focused verification:** `node --test tests/flow-assets-deploy.test.mjs tests/flow-assets-restore.test.mjs tests/install.test.mjs`. **Runtime harness:** temporary Git repository plus destination fixture; expect focused command exit 0 after GREEN. **Rollback:** revert `managed-deployment` routing and deploy test changes; use the existing verified v1 restore path for later real recovery, never a live import. **Estimate:** 350–400 lines. **Commit/review unit:** `feat(assets): deploy verified OpenCode host mappings`.

- [x] **12. Keep `install.mjs` as a bounded OpenCode compatibility adapter.** **Depends on:** 11. **Files:** `install.mjs`, `tests/install.test.mjs`, `README.md`, and `hosts/opencode/flow-assets.json` only if its documented bootstrap mapping changes. **RED:** add assertions that no-argument preview emits `host: "opencode"`, `--host pi` fails with Pi package guidance, apply still requires target commit and plan ID, and no real host configuration is read/written; run `node --test tests/install.test.mjs`. **GREEN:** route preview/apply through v2 OpenCode deployment while preserving no-argument read-only preview and one-release compatibility behavior. **TRIANGULATE:** test arbitrary cwd/destination precedence and stale target/plan identities. **REFACTOR:** keep installer argument parsing thin and delegate all ownership logic to the asset engine. **Focused verification:** `node --test tests/install.test.mjs tests/flow-assets-deploy.test.mjs`; then `node --test tests/*.test.mjs` for PR 6. **Runtime harness:** isolated installer process fixture; expect focused command exit 0 after GREEN. **Rollback:** revert installer compatibility text/routing only; do not add a Pi file-copy path. **Estimate:** 220–300 lines. **Commit/review unit:** `feat(installer): retain bounded OpenCode migration path`.

## PR 7 — Exceptional Reconciliation, Legacy Migration, and Release Gate (target: three units, each 260–390 lines)

- [x] **13. Replace routine snapshot authority with explicit adapter-only reconciliation.** **Depends on:** 10–12. **Files:** add `tools/lib/reconciliation.mjs`; refactor `tools/flow-assets.mjs`; add `tests/flow-assets-reconcile.test.mjs`; remove or retire snapshot paths in `scripts/flow-skills.mjs`, `skills/flow-skills-sync/SKILL.md`, and `commands/flow-skills-sync.md`. **RED:** assert reconcile requires explicit `--host`, absolute `--source`, and `--dry-run`; proves ordinary verify/deploy/install/package discovery never invokes reconciliation; and proves no repository write happens before matching repository commit/plan ID and approval boundary; run `node --test tests/flow-assets-reconcile.test.mjs`. **GREEN:** implement `reconcile --host pi|opencode --source <absolute-path> --dry-run` and identity-bound apply that imports only approved adapter paths, regenerates the full lock set, and neither commits/pushes/publishes/deploys nor mutates host state. **TRIANGULATE:** test shared skill/runtime/manifest/lock differences are report-only, stale/concurrent source or repository drift rejects safely, and injected failure restores repository preimages with recovery evidence. **REFACTOR:** reuse transaction primitives only through narrow repository-write interfaces. **Focused verification:** `node --test tests/flow-assets-reconcile.test.mjs tests/flow-assets-deploy.test.mjs tests/install.test.mjs`. **Runtime harness:** isolated repository/live-source fixture; expect focused command exit 0 after GREEN. **Rollback:** revert reconciliation module/router and restore only the prior adapter UX; never run reconciliation to undo it. **Estimate:** 350–390 lines. **Commit/review unit:** `feat(assets): require explicit host reconciliation`.

- [x] **14. Publish the complete migration matrix and remove legacy surface claims.** **Depends on:** 6, 8–9, and 13. **Files:** `README.md`, `CHANGELOG.md`, add `docs/multihost-migration.md`, remove `commands/flow-auto-deliver.md`, remove `commands/flow-figma.md`, remove `commands/flow-skills-sync.md`, remove `skills/flow-skills-sync/SKILL.md`, update `core/workflows.json`, `hosts/opencode/flow-assets.json`, and add/extend `tests/legacy-migration.test.mjs`. **RED:** test every current command and skill has a retained/replaced/removed status; reject legacy aliases from the v1 public registry/package/adapter mapping; run `node --test tests/legacy-migration.test.mjs`. **GREEN:** document Pi first, supported workflow matrix, host UX/ownership differences, `pi install` versus OpenCode preview/apply, exact legacy dispositions, and no-blind-replacement guidance; remove only specified rejected surfaces and stale README `flow-finish`/`flow-release` claims. **TRIANGULATE:** assert a retained workflow is listed for both hosts and removed names return documented migration guidance rather than a hidden substitute. **REFACTOR:** use one concise table for recognition; keep technical documentation in English. **Focused verification:** `node --test tests/legacy-migration.test.mjs tests/pi-package.test.mjs tests/opencode-adapter.test.mjs`. **Runtime harness:** N/A—documentation and contract mapping only. **Rollback:** restore only removed legacy adapters/skills and the preceding migration table version; never replace a host tree. **Estimate:** 260–350 lines. **Commit/review unit:** `docs(multihost): publish host migration matrix`.

- [x] **15. Regenerate release-consistency artifacts and perform the repository-only release gate.** **Depends on:** 1–14. **Files:** generated `hosts/pi/flow-assets.lock.json`, `hosts/opencode/flow-assets.lock.json`, `flow-generation.lock.json`; `README.md`, `CHANGELOG.md`, `.gitattributes`, and affected tests only if verification exposes a legitimate contract gap. **RED:** deliberately tamper one generated lock record in a temporary fixture and show its targeted verification fails; run `node --test tests/flow-assets-generation.test.mjs`. **GREEN:** regenerate the three locks once from a clean repository state and verify package contents, both host identities, shared generation, and all host mappings. **TRIANGULATE:** repeat generation in a clean temporary checkout/fixture and assert byte-identical lock output for identical inputs. **REFACTOR:** do not refactor product behavior at the release gate; only correct proven generator/contract defects in their owning task and rerun its focused suite. **Focused verification:** `node --test tests/flow-assets-generation.test.mjs tests/pi-package.test.mjs tests/opencode-adapter.test.mjs tests/flow-assets-deploy.test.mjs tests/flow-assets-reconcile.test.mjs tests/legacy-migration.test.mjs`, then `node --test tests/*.test.mjs`. **Runtime harness:** local packed-package and temporary deployment/reconciliation fixtures only; expect command exit 0, report literal skip reasons separately, and do not publish or deploy live hosts. **Rollback:** revert the coherent reviewed repository generation; never rewrite historical locks or infer a live-host import. **Estimate:** 120–220 generated/verification lines; include complete lock snapshots in review identity. **Commit/review unit:** `chore(release): verify multihost repository generation`.

## Completion Criteria

- [x] Every v1 workflow in `core/workflows.json` has validated portable resources, declared Pi package discovery, and a thin OpenCode adapter claim.
- [x] The full suite `node --test tests/*.test.mjs` exits 0; capability skips are recorded separately with reasons.
- [x] Package publication, `pi install`, OpenCode deployment, reconciliation apply, Git commit/push, and release creation remain separate human-authorized operations.
- [x] Apply evidence names each completed work unit, exact focused/full command result, runtime-harness result or `N/A` reason, rollback boundary, changed-line count, and any remaining delivery decision.
