---
description: Prepare, execute, or recover a bounded Flow Debt transition.
agent: flow-debt-agent
subtask: true
---

Arguments: `$ARGUMENTS`

Load and follow `~/.config/opencode/skills/flow-debt/SKILL.md` before responding.

The dedicated adapter treats `$ARGUMENTS` only as data. It owns OpenCode's native approval boundary and invokes the packaged runtime; it never interpolates arguments into shell syntax or emulates filesystem access.
