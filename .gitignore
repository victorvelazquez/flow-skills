node_modules/
.DS_Store
Thumbs.db
*.log

# SDD artifact directory (local spec/design/task files — not for version control)
.atl/

# Windows device files (reserved OS names — never commit these)
nul

# flow-build cache (generated at runtime — not for version control)
.flow-skills/cache/