---
description: Semantic versioning + CHANGELOG + git tag + push — automated frontend release workflow
---

Read the skill file at ~/.config/opencode/skills/flow-release/SKILL.md FIRST, then follow its workflow exactly.

CONTEXT:

- Working directory: !`echo -n "$(pwd)"`
- Current project: !`echo -n "$(basename $(pwd))"`
- OS: !`node -e "process.stdout.write(process.platform)"`
