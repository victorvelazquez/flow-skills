---
description: Figma to React sync — extrae specs pixel-perfect de un nodo Figma (colores, tipografía, spacing, layout) y los aplica a un componente React/MUI respetando los tokens del tema.
---

Read the skill file at ~/.config/opencode/skills/flow-figma/SKILL.md FIRST, then follow its workflow exactly.

CONTEXT:

- Working directory: !`echo -n "$(pwd)"`
- Current project: !`echo -n "$(basename $(pwd))"`
- OS: !`node -e "process.stdout.write(process.platform)"`
