---
description: Universal project documentation builder — 11-phase interactive workflow for backend, frontend, mobile, and fullstack projects.
---

Read the skill file at ~/.config/opencode/skills/flow-build/SKILL.md FIRST, then follow its workflow exactly.

CONTEXT:

- Working directory: !`node -e "process.stdout.write(process.cwd())"`
- Current project: !`node -e "const path=require('path');process.stdout.write(path.basename(process.cwd()))"`
- OS: !`node -e "process.stdout.write(process.platform + '/' + process.arch)"`
