# Fix Flow PR intent authoring failure

## Objective and evidence

Two fresh manual `/flow-pr` attempts on committed task branch `feat/flow-branch-direct-selection` failed before push/PR creation, at `intent-read` with a privacy-safe `runtime-failure` / `unexpected-error`. Read-only reconstruction of each agent's recorded template and exact `edit` replacements proves the edited `intent.json` was invalid JSON: the edited `body` line ended without its original comma immediately before the unchanged `draft` line (`JSON.parse` failed at line 5). This is an agent authoring defect, not evidence of repository drift or a GitHub failure. The agent logs contain private draft text; persist only structural findings, never intent contents, handles, or temp paths.

## Scope and constraints

Fix the root class at the shared skill and Pi executing-agent instructions: semantic value-only edits must preserve the template's JSON delimiters, and the agent must verify syntax before finalizing. Preserve operational fields, host permissions, privacy-safe diagnostics, manual push/PR authorization, and no Jira mutation. Prefer the smallest correction rather than a second approval or a new authoring mode. Consider reusing existing `invalid-contract` for a malformed intent only if it improves the failure exit without broadening error disclosure; keep generic unexpected-error coverage. Do not run live PR prepare/execute, push, or Jira as a verification probe. Tests may use isolated temporary repositories and mocked GitHub. A real PR needs a later fresh explicit `/flow-pr` invocation.

## Work units

- [x] F1 — Reproduce the recorded comma-loss pattern in an isolated test, then tighten shared skill and Pi named-agent authoring guidance to edit JSON scalar values only, preserve punctuation, and read back/validate syntax before finalize. If runtime classification changes, keep static privacy-safe errors and a runnable recovery. Route: one bounded multi-file writer. Acceptance: recorded edit shape fails before the fix, safe edit preserves the template and finalizes in fixture, operational fields unchanged.
- [x] F2 — Reconcile source/package locks, activate affected installed Pi/OpenCode copies with backups, and independently verify focused fixture/adapter/package checks. Native assessment was unassessable because this task document is untracked, so a separate read-only verifier ran. The installed Pi agent preserves its manager-owned model/thinking frontmatter and normalizes exactly to the source. Backup: `~/.pi/agent/backups/flow-pr-intent-authoring-activation-2026-09-25T21-47-58-405Z/`.

F2 independent evidence: focused Flow PR fixtures 10/10 passed (2 top-level, 8 nested, no skips); agent/adapter contracts 43/43 passed (no skips). Root/Pi/OpenCode asset verification passed (59/73/83 records), 75-entry `npm pack --dry-run --json` passed, and `git diff --check` passed with LF/CRLF warnings only. Pre/post status and SHA256 were identical for all 20 inspected paths: nine tracked modifications and this untracked task document, no unexpected path. Installed Pi agent (after removing only manager model/thinking) and installed OpenCode agent/skill match source; backup manifest matched three before/after hashes. A pi-lens autofix notice for `tests/flow-pr.test.mjs` has unknown attribution; raw SHA256 `dd3845ce...` stayed equal before/after both test commands, its only diff hunk is the intended 52-line fixture, and no asset lock records this test path. No live Flow PR, push, PR, merge or Jira operation was used as a probe. The worktree is uncommitted; publication requires fresh explicit `/flow-commit` followed by `/flow-pr` invocations.

F1 evidence: isolated fixture reproduced dropped body comma as `SyntaxError` and runtime `intent-read` failure without GitHub mutation; value-only replacement preserved field order/operational values, JSON-escaped newline and quotes, and finalized `prepared`. Focused Flow PR pattern passed 10/10; agent/adapter contracts passed 43/43. Shared skill and host agents now require delimiter-preserving scalar edits and pre-finalize readback. Root, Pi, and OpenCode locks verified locally at F1; F2 activation and independent verification are recorded above. No private draft, handle, or temp path was copied into artifacts.

ODD TDD: no active organic strict-TDD setting observed in the prior work; runner is Node's `node --test` with bounded test-name patterns for the large Flow PR suite. Forecast under about 200 authored diff lines excluding generated locks, delivery strategy `ask-on-risk`. Rollback boundary: only Flow PR authoring instructions, focused tests, narrowly justified runtime error classification if changed, derived asset locks, and backed-up installed copies. The current branch is clean at `3752d42` before this work; no commit, push, PR, merge or Jira mutation is authorized as a probe. A work-unit commit or publication requires a fresh explicit workflow invocation.
